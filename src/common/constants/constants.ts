export enum PATH {
  LOGIN = '/',
  PROFILE = '/profile',
}
export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password',
  CHECKBOX = 'checkbox',
}
export enum InputName {
  LOGIN = 'login',
  PASSWORD = 'password',
  REMEMBER = 'rememberMe',
}
